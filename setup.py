from setuptools import setup, find_packages

setup(
    name='LodeRunner',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    author='Reid Williams',
    description='LodeRunner game as a Python package',
)
